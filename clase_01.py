import numpy as np
import matplotlib.pyplot as mplt
from scipy.io import wavfile

""" 
Cada numero trabajado aqui son BITS

range(a,b,c)
a = start, b = end c = steps

mplt.plot(X,Y) continuas
mplt.stem(X,Y) discretas

x = [i+2 for i in range(10) if i%2== 0]
    [haz esto for este rango si esto se cumple]

n = np.arange(-2,3)     # Crea un array desde -2 a 2
u = np.heaviside(n,1)   # no se
"""

amplitud = 1.0
freqM = 44100          #Frequencia de muestreo (cada cuanto toma muestra)

""" 
def ondaSeno(tiempo,freq):
    x = []
    muestras = tiempo * freqM
    for t in range()
mplt.show()
"""

print(amplitud)